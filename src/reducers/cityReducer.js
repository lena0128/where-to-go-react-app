const initialState = {

}

const cityReducer = (initialState, action) => {
   switch(action.type) {
       case "ADD_CITY":
           return {

           };

        default:
            return state;    
   }
}